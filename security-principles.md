# Security Principles — misstore

このストアのセキュリティ原則。**ロック文書**であり、機能追加・仕様変更の提案は
実装前に必ず本文書と照合すること。原則に反する機能は、UX・利便性・要望の多さを
理由に採用しない。原則自体の変更は、本文書の明示的な改定 PR としてのみ行う。

## アーキテクチャ前提(2026-08 確定)

misstore は次の 4 点に単純化された。原則はこの構成を守る:

1. **配布は GitHub PR のみ。** アイテムの投稿・更新は GitHub の Pull Request で
   行い、作者の同一性は GitHub アカウントが担保する。**Misskey ログインは無い。**
2. **審査は GitHub Actions(CI)+ 高権限アイテムのみ人手。** 機械的チェックを
   全 PR で強制し、危険候補フラグが立ったものだけ人間が読む。
3. **利用状況(誰がどのアイテムを使うか)はストアが持たない。** NoteDeck
   クライアント側がローカルに所有し、バックアップはユーザーがテキストで書き出す。
   データ主権・プライバシー主権はユーザー側にある。
4. **misstore はユーザーデータを一切保持しない。** アカウントもログインも
   トークンもライブラリも無い。ストアは「静的レジストリ + CI」だけである。

この単純化により、認証・セッション・サーバー側個人情報にまつわる攻撃面は
**存在ごと消えた**(過去の赤チーム検証 docs/design/redteam-log.md を参照)。
攻撃面を防御するのではなく、攻撃面の存在自体を無くすのが本設計の背骨である。

## 位置づけ — 原則と仕様の区別

- **原則(本文書)**: どんな批判的検証にも揺らがない不変条項。
- **仕様(docs/ 以下)**: 原則から導出される実装方針。閾値・CI チェック内容などは
  状況に応じて調整してよいが、原則に抵触してはならない。
- **外部依存**: Misskey のインストール確認画面、AiScript/NoteDeck のホスト関数の
  挙動等は misstore が制御できない。監視対象とし、変化したら仕様側で吸収する。

これらの原則が「揺らがない」のは、**防御ではなく能力の不在**として書かれている
からである。ただし赤チーム検証で、この不変性には 3 つの前提があることが判明した:

1. **能力の不在は「主張」でなく「継続的に検証される事実」である。** 「存在しない」と
   信じた経路が実在した例がある(NoteDeck の `Nd:http` は汎用ネットワーク機能で、
   P1/P4 が想定した『実質 `Mk:api` に限定』を最初から破っていた)。不在は CI と
   レジストリ棚卸しで測り続ける対象である。
2. **各原則の射程を利用者視点で明記する。** 「misstore が○○を持たない」ことと
   「利用者が安全」であることは別。エコシステム(NoteDeck の Vault・`Nd:http`)の
   リスクは misstore の原則の外にある。
3. **原則は CI に翻訳されて初めてロックになる。** 単独運営では原則を書く人・実装
   する人・レビューする人が同一で、「改定 PR を要する」は自分への規律にすぎない。
   規律は疲労に負ける(xz-utils, event-stream)。**機械が強制しない原則は決意表明
   である。** 各条項は 1:1 対応する CI チェックを持つべき(docs/design/security.md
   の実装状態欄)。

---

## P1. 配布物はソースそのものである

配布されるのは人間可読な正規形テキスト(AiScript / JSON5 / Markdown / 検証済み
SVG)であり、レビューされた文字列がバイト単位でそのまま利用者に届く。

**禁じること**: ビルド工程の導入、バイナリ・minify 済み・生成済みアセットの
受け入れ、レジストリ外(外部 CDN・外部 URL)への実体の配置。

**破れない理由**: ビルド供給網攻撃・バイナリ差し替え・レビューと配布物の乖離は、
ビルドとバイナリが存在しなければ成立しない。ただし「レビュー対象 = 配布物」は
生成物(`api.json` 等)を git にコミットすると崩れる(赤チーム R1-C)。生成物は
git から追放し、配布バイトはソースからのみ導出する(仕様 S11)。

## P2. 公開経路は GitHub PR ただ一本である

アイテムの公開の唯一の入口は GitHub の Pull Request → 保護された main への
マージ → 静的ビルド → デプロイ。API・Functions・DB などの動的層は存在せず、
レジストリへの書き込み経路も存在しない。全公開・全変更は公開 git 履歴に残る。

**正直な限界(赤チーム反証)**: 配信バイトの source of truth は git でなく
Cloudflare の asset store であり、Cloudflare トークン保有者は `wrangler deploy` で
git を迂回して任意配信できる(履歴に残らない)。この一点だけは「防御」であると認める。

**禁じること**: main への直接 push、人間が長命デプロイトークンを保持すること。

**担保**: デプロイは Workers Builds(git 連携)からのみ、トークンは単一 Worker
スコープに限定。main は署名コミット + force-push 禁止 + self-merge 禁止。git 迂回
デプロイの検出は、期待ハッシュ(git 由来)と実配信バイトの定期突合で行う(仕様 S1。
規模に応じ、当面は CI ないし手動突合で足りる — 常設の外部モニタは過剰)。

## P3. 公開済みバージョンは不変である

一度公開された (id, version) の内容は変更しない。修正は必ず新バージョンとして
公開ゲートを再通過する。アイテムへの参照は常に内容ハッシュ(sha512)で固定される。

**禁じること**: 同一バージョンの内容差し替え、ハッシュ検証を伴わない参照、
利用者の明示的な確認なしに内容が入れ替わる自動更新・live fetch 経路。

**破れない理由**: 「信頼を得てから中身を替える」型の攻撃は、参照が内容に固定されて
いれば必ず新バージョンとしてゲートと利用者の目の前を通る。

## P4. レビュー可能性そのものが掲載条件である

掲載の条件は「安全と判定されたこと」ではなく「安全性を判定できるテキストで
あること」。難読化・不可視 Unicode・解析を妨げる構造は、悪意の有無を問わず
掲載しない。安全性の立証責任は投稿側にある。

判定可能性の中核は**権限の静的導出**である。ホスト関数(`Mk:api` / `Nd:call` /
`Nd:http` / `Mk:save` 等、外部作用を持つ全ての入口)は `関数名(リテラル, …)` の
構文形でのみ出現してよい。変数束縛・引数渡し・オブジェクト格納は導出を破綻させる
ため reject する。導出対象は特定名前空間に限定せず全ホスト関数とする(赤チーム:
実際の流出経路は `Mk:api` でなく `Nd:call("vault.fetch")` / `Nd:http` だった)。

**禁じること**: 読めないコードの掲載、見た目と実体が乖離しうるテキスト、ホスト関数の
エイリアス・間接呼び出し、導出網に含まれないホスト関数の存在。

**破れない理由**: 検出器と回避の競争をしない。判定不能なものは負けでなく門前払い。
ただし静的導出には天井があり(config 駆動の引数・第2引数の宛先など)、そこは
CI が危険候補フラグを立て**人間が読む**(仕様 S10)。「導出網に漏れがない」ことは
CI で測り続ける前提(冒頭の前提 1)。

## P5. ストアはインストールの実行者にならない

misstore が提供するのは「何が入るか」の完全な開示(全文・導出された権限・
ハッシュ)までであり、利用者のアカウントへの適用は NoteDeck / Misskey 側の確認を
必ず通る。ストアはインストールを代行しない。

**禁じること**: Misskey のインストール確認画面のバイパス、確認を省略する
「ワンクリック/一括」機能。

**破れない理由**: クライアント側の確認は、ストア自身が侵害された場合に利用者を
守る最後の防壁である。ストアがそれを迂回する能力を持たなければ、ストアの侵害は
アカウントの侵害に直結しない。

## P6. misstore はユーザーデータを保持しない

アカウント・ログイン・アクセストークン・利用ライブラリのいずれも保持しない。
ストアは静的レジストリと CI だけで構成され、利用者に紐づくデータを一切持たない。

**禁じること**: サーバー側でのアカウント/セッション/トークン/利用状況の保存
(「暗号化して保存」を含む)。ログイン機能そのものの安易な追加。

**破れない理由**: 存在しないものは漏れない。ユーザーデータが無ければ、DB 侵害・
法的開示・運営者の閲覧・相関による deanonymization のいずれも成立しようがない。
これは「トークンを消す」より強い — **持つべきものが最初から無い**。

**射程の明記**: P6 は misstore の管轄のみを保証する。利用者のアカウント安全性は
エコシステム側(NoteDeck の Vault・`Nd:http` の送信先)に依存し、そこは P4(権限
導出)と P7(失効)で受ける。将来ログイン/評価機能を検討する場合は、本原則の
改定 PR を要し、赤チーム R1-1〜13 の認証系反証を必ず再検討する。

## P7. いかなる単一防壁も信用しない

審査は破られる前提、運営アカウントは乗っ取られる前提、原則の実装にもバグがある
前提で設計する。失効手段(revocation)と被害限定(最小権限の可視化)を常に併設し、
ある防壁の強化を理由に別の防壁を外さない。

**禁じること**: 「審査を通ったから安全」を前提とする機能、失効手段を持たない
配布形態、権限表示の省略。

**破れない理由**: これは他の 6 原則が破られた日のための原則である。P1–P6 が能力の
不在で成立するのに対し、P7 は「不在にできない残余リスクは必ず受け皿を持つ」ことを
強制する。

---

## 改定について

原則の削除・緩和を含む PR は、対応する攻撃クラスが再び成立することを本文書に
明記しなければならない。「当面問題ないため」「利便性のため」は改定理由として
認めない。

冒頭の前提 3 のとおり、単独運営下ではこの手続きは自分への規律にすぎない。実効的な
歯止めは文章でなく **CI** である: 各原則に対応する CI チェック(docs/design/
security.md の実装状態欄)を無効化・緩和する変更は、その原則の改定 PR と同格に
扱う。**チェックを外すコミットが、原則を緩める行為そのものである。**
