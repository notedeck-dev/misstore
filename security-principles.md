# Security Principles — misstore

このストアのセキュリティ原則。**ロック文書**であり、機能追加・仕様変更の提案は
実装前に必ず本文書と照合すること。原則に反する機能は、UX・利便性・要望の多さを
理由に採用しない。原則自体の変更は、本文書の明示的な改定 PR としてのみ行う。

## 位置づけ — 原則と仕様の区別

- **原則(本文書)**: どんな批判的検証にも揺らがない不変条項。すべて misstore
  自身の管理下にあり、規模・言語仕様・運用体制の変化に依存しない。
- **仕様(docs/ 以下)**: 原則から導出される実装方針。閾値・審査 Tier・
  レビュー体制などは状況に応じて調整してよい。ただし調整の結果が原則に
  抵触してはならない。
- **外部依存(前提であって保証ではない)**: Misskey のインストール確認画面、
  AiScript が汎用ネットワーク機能を持たないこと等は misstore が制御できない。
  これらは監視対象とし、変化したら仕様側で吸収する。原則はそれでも変わらない。

これらの原則が「揺らがない」のは、**防御ではなく能力の不在**として書かれている
からである。防御は破られうるが、存在しない経路・保持していない秘密・挟まれて
いない工程は攻撃のしようがない。原則を破れる主体は攻撃者ではなく、将来の設計
判断である。本文書はそれを防ぐためにある。

ただし赤チーム検証(2026-08、docs/design/redteam-log.md)で、この主張には
3 つの前提があることが判明した。原則の不変性はこれらが満たされる限りにおいて
のみ成立する:

1. **能力の不在は「主張」ではなく「継続的に検証される事実」である。** 「存在
   しない」と信じて書いた経路が実際には存在した例がある(NoteDeck の `Nd:http`
   は汎用ネットワーク機能であり、P1/P4 が想定した『実質 `Mk:api` に限定』を
   最初から破っていた)。不在は CI とレジストリ棚卸しで**測り続ける**対象であり、
   一度書けば保たれるものではない。
2. **原則は「misstore」を主語に切られているが、利用者はその主語で切られた
   リスクを負わない。** 「misstore はトークンを保持しない(P6)」は真だが、
   利用者のアカウントが安全であることは意味しない(エコシステムの Vault・
   `Nd:http` 経由で漏れうる)。各原則の**射程**を利用者視点で明記する。
3. **原則は CI に翻訳されて初めてロックになる。** 単独運営では原則を書く人・
   仕様を書く人・実装する人・レビューする人・マージする人が同一人物であり、
   「改定 PR を要する」は自分に対して自分が課す規律にすぎない。規律は疲労と
   締切に負ける(xz-utils, event-stream はいずれも誠実な単独メンテナの疲弊が
   起点だった)。**機械が強制しない原則は、原則ではなく決意表明である。**
   各条項は 1:1 対応する CI チェックを持たねばならない(docs/design/security.md
   の実装状態欄を参照)。

---

## P1. 配布物はソースそのものである

配布されるのは人間可読な正規形テキスト(AiScript / JSON5 / Markdown / 検証済み
SVG)であり、レビューされた文字列がバイト単位でそのまま利用者に届く。

**禁じること**: ビルド工程の導入、バイナリ・minify 済み・生成済みアセットの
受け入れ、レジストリ外(外部 CDN・外部 URL)への実体の配置。

**破れない理由**: ビルド供給網攻撃・バイナリ差し替え・レビューと配布物の乖離は、
ビルドとバイナリが存在しなければ成立しない。

## P2. Git が唯一の正規公開経路であり、逸脱は検出される

アイテムの公開の**正規経路**は「保護された main ブランチへのマージ → 静的ビルド
→ デプロイ」のみ。API・Functions・DB などの動的層は、いかなる権限でもレジストリ
へ書き込めない。全公開・全変更は公開 git 履歴に残る。

**正直な限界(赤チーム反証により訂正)**: 配信バイトの source of truth は git
ではなく Cloudflare の asset store であり、git はその**入力**にすぎない。Cloudflare
API トークンを持つ者は、`wrangler deploy` で git を迂回して任意の内容を配信でき、
これは git 履歴に残らない。したがって P2 は「能力の不在」では書けない — デプロイ
平面(Cloudflare アカウント + トークン)という第二の経路が構造的に存在する。
この経路だけは「防御」であることを認め、**検出**で担保する。

**禁じること**: 動的層からレジストリへの書き込み経路、main への直接 push、
人間が長命デプロイトークンを保持すること(ローカル `wrangler deploy` の常用)。

**担保の方法**: (a) デプロイは Workers Builds(git 連携)からのみ行い、トークンは
単一 Worker スコープに限定。(b) **別権限ドメイン(別 Cloudflare アカウント / 別
GitHub リポジトリ)の外部モニタ**が「`git clone → registry:build` で算出した期待
ハッシュ」と「実配信バイトの sha512」を定期突合し、不一致を通報する。これが
Certificate Transparency における monitor の役割であり、これ無しでは git 迂回
デプロイは永久に無検知になる。(c) main の署名コミット必須 + force-push 禁止。

**この範囲で破れない理由**: 動的層が完全に侵害されても、攻撃者が得るのは「PR を
作る能力」までであり、配布能力ではない。デプロイ平面の侵害による git 迂回配信は
外部モニタが検出し、git 履歴の改竄は署名と外部モニタが検出する。

## P3. 公開済みバージョンは不変である

一度公開された (id, version) の内容は変更しない。修正は必ず新バージョンとして
公開ゲートを再通過する。アイテムへの参照は常に内容ハッシュ(sha512)で固定される。

**禁じること**: 同一バージョンの内容差し替え、ハッシュ検証を伴わない参照、
利用者の明示的な確認なしに内容が入れ替わる自動更新・live fetch 経路。

**破れない理由**: 「信頼を得てから中身を替える」型の攻撃(更新経由・掲載後
差し替え)は、参照が内容に固定されていれば、必ず新バージョンとして
ゲートと利用者の目の前を通ることになる。

## P4. レビュー可能性そのものが掲載条件である

掲載の条件は「安全と判定されたこと」ではなく「安全性を判定できるテキストで
あること」。難読化・不可視 Unicode・解析を妨げる構造は、悪意の有無を問わず
掲載しない。安全性の立証責任は投稿側にある。

判定可能性の中核は**権限の静的導出**である。ホスト関数(`Mk:api` / `Nd:call` /
`Nd:http` / `Mk:save` 等、クライアントが提供する全ての外部作用の入口)は、必ず
`関数名(リテラル, …)` の構文形でのみ出現してよい。これらを変数に束縛する・
引数として渡す・オブジェクトに格納するコードは、第一引数が動的化され導出が
破綻するため、悪意の有無を問わず reject する。導出対象は特定の名前空間
(`Mk:` 等)に限定せず、**外部作用を持つ全ホスト関数**とする(赤チーム反証:
実際の流出経路は `Mk:api` でなく `Nd:call("vault.fetch")` / `Nd:http` だった)。

**禁じること**: 読めないコードの「スキャンで問題なし」を理由とする掲載、
見た目と実体が乖離しうるテキスト(双方向制御文字・ゼロ幅文字等)の受け入れ、
ホスト関数のエイリアス・間接呼び出し、導出網に含まれないホスト関数の存在。

**破れない理由**: 検出器と回避の競争(先例ストアが敗け続けている戦い)を
しない。判定不能なものは負けではなく門前払いであり、回避のインセンティブ
ごと消える。ただし「導出網に漏れがない」ことは P4 の前提であり、新しい
ホスト関数の追加を監視し続ける限りにおいて成立する(冒頭の前提 1)。

## P5. ストアはインストールの実行者にならない

misstore が提供するのは「何が入るか」の完全な開示(全文・導出された権限・
ハッシュ)までであり、利用者のアカウントへの適用はクライアント側の確認を
必ず通る。

**禁じること**: Misskey のインストール確認画面のバイパス、ストア側権限による
利用者アカウントへの直接書き込み、確認を省略する「ワンクリック/一括」機能。

**破れない理由**: クライアント側の確認は、ストア自身が侵害された場合に利用者を
守る最後の防壁である。ストアがそれを迂回する能力を持たなければ、ストアの侵害は
アカウントの侵害に直結しない。

## P6. 漏洩しうる秘密を保持しない

利用者の Misskey アクセストークンは認証完了後に即時破棄し、永続化しない。
将来トークンを要する機能を作る場合も、その場で取得し用が済み次第破棄する。

**禁じること**: トークン・資格情報の DB 保存、「暗号化して保存」を含む
あらゆる永続化。ログ・URL クエリ・エラーレスポンス・Referer など DB 以外の
経路への漏出も同様に禁じる(破棄は「DB に入れない」だけでは足りない)。

**射程の明記(赤チーム反証により追加)**: P6 の主語は misstore であり、
「misstore の DB に秘密が無い」ことしか保証しない。以下は P6 の射程外であり、
別の受け皿を要する:
- **ライブラリ等の非トークン個人情報**は P6 では守られない。「誰がどの拡張を
  使っているか」+ acct は個人情報であり、DB 侵害で漏れる。→ 保存項目の最小化
  と PRIVACY 開示で対処(P6 ではなく仕様 S7/S14 の管轄)。
- **エコシステム側の秘密**(NoteDeck の Secret Vault が保持する第三者トークン、
  `Nd:http` の送信先)は misstore の管轄外だが、misstore が配布したアイテム・
  skill がそれを漏らしうる。→ 権限導出(P4)と失効(P7)で対処。

**破れない理由**: 存在しないものは漏れない。トークン DB がなければ、
データベースの完全な侵害でも**トークンには**到達できない。ただしこれは
「利用者のアカウントが安全」を意味しない — 上記射程外の経路が残る。

## P7. いかなる単一防壁も信用しない

審査は破られる前提、運営アカウントは乗っ取られる前提、原則の実装にも
バグがある前提で設計する。失効手段(revocation)と被害限定(最小権限の
強制と可視化)を常に併設し、ある防壁の強化を理由に別の防壁を外さない。

**禁じること**: 「審査を通ったから安全」を前提とする機能、失効手段を持たない
配布形態、権限表示の省略。

**破れない理由**: これは他の 6 原則が破られた日のための原則である。
P1–P6 が能力の不在によって成立するのに対し、P7 は「不在にできない残余
リスクは必ず受け皿を持つ」ことを強制する。

---

## 改定について

原則の削除・緩和を含む PR は、対応する攻撃クラスが再び成立することを
本文書に明記しなければならない。「当面問題ないため」「利便性のため」は
改定理由として認めない。

ただし冒頭の前提 3 のとおり、単独運営下ではこの改定手続きは自分への規律に
すぎない。したがって改定の実効的な歯止めは文章ではなく **CI** である:
各原則に対応する CI チェック(docs/design/security.md の実装状態欄)を
無効化・緩和する変更は、その原則の改定 PR と同格に扱う。**チェックを外す
コミットが、原則を緩める行為そのものである。**
